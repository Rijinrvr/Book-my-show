import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import { AuthProvider } from '@/context/AuthContext';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'BookMyShow - Cinema Ticket Booking',
  description: 'Book your favorite movies at the best theaters near you. Easy seat selection, instant confirmation.',
  keywords: 'cinema, movie tickets, book tickets, theater, seat selection',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <AuthProvider>{children}</AuthProvider>
      </body>
    </html>
  );
}
